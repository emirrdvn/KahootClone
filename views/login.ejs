<!DOCTYPE html><html lang="tr">
  <head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <%- include('partials/_head_content', { title: 'Sayfa Başlığı' }) %>
  <link rel="stylesheet" href="/css/style.css">
  <!-- Font Awesome CDN -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap'); /* mainscreen'den font importu */

    body {
      font-family: 'Montserrat', sans-serif; /* mainscreen'deki fontu kullan */
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      /* mainscreen.ejs'den kopyalanan arka plan stilleri */
      color: #333; /* mainscreen'deki gibi genel metin rengi */
      background: linear-gradient(-45deg, #ff8a00, #e52e71, #833ab4, #23a6d5, #23d5ab, #6a11cb, #2575fc);
      background-size: 500% 500%;
      animation: gradientAnimation 25s ease infinite;
      overflow-x: hidden; /* mainscreen'deki gibi */
    }

    /* mainscreen.ejs'den kopyalanan animasyon */
    @keyframes gradientAnimation {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }

    /* Added style for the icon above the H1 heading */
    .login-container h1::before {
      content: "\f2bd"; /* Font Awesome user-circle icon */
      font-family: "Font Awesome 5 Free";
      font-weight: 900; /* Required for solid Font Awesome icons */
      display: block; /* Makes the icon appear on its own line */
      font-size: 48px; /* Adjust size as needed, e.g., 1.5x the h1 font-size */
      margin-bottom: 15px; /* Space between the icon and the "Giriş Yap" text */
      color: #007bff; /* Icon color, matches the button and link color */
      /* text-align: center; is inherited from .login-container for block elements */
    }
    .login-container {
      background-color: rgba(255, 255, 255, 0.9); /* Arka planla daha iyi uyum için hafif şeffaflık veya opak beyaz */
      padding: 40px;
      border-radius: 10px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.2); /* Gölgeyi biraz daha belirgin yapabiliriz */
      width: 100%;
      max-width: 400px;
      text-align: center;
    }
    .login-container h1 {
      color: #333;
      margin-bottom: 25px;
      font-size: 28px;
    }
    .input-group {
      position: relative;
      margin-bottom: 20px;
    }
    .input-group .input-icon {
      position: absolute;
      left: 15px;
      top: 50%;
      transform: translateY(-50%);
      color: #888;
    }
    .input-group input {
      width: calc(100% - 40px); /* İkon için yer bırak */
      padding: 12px 12px 12px 40px; /* Sol padding ikona göre ayarlandı */
      border: 1px solid #ddd;
      border-radius: 6px;
      font-size: 16px;
      transition: border-color 0.3s;
    }
    .input-group input:focus {
      outline: none;
      border-color: #007bff;
    }
    .login-button {
      background-color: #007bff;
      color: white;
      border: none;
      padding: 12px 20px;
      font-size: 16px;
      border-radius: 6px;
      cursor: pointer;
      width: 100%;
      transition: background-color 0.3s;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .login-button i {
      margin-right: 8px;
    }
    .login-button:hover {
      background-color: #0056b3;
    }
    .register-link {
      margin-top: 25px;
      font-size: 14px;
    }
    .register-link a {
      color: #007bff;
      text-decoration: none;
    }
    .register-link a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  
  <div class="login-container">
    <h1>Giriş Yap</h1>
    <form action="/login" method="POST">
      <div class="input-group">
        <span class="input-icon"><i class="fas fa-user"></i></span>
        <input type="text" name="username" placeholder="Kullanıcı Adı" required>
      </div>
      <div class="input-group">
        <span class="input-icon"><i class="fas fa-lock"></i></span>
        <input type="password" name="password" placeholder="Şifre" required>
      </div>
      <button type="submit" class="login-button">
        <i class="fas fa-sign-in-alt"></i> Giriş Yap
      </button>
    </form>
    <div class="register-link">
      Hesabın yok mu? <a href="/register">Kayıt Ol</a>
    </div>
    <br>
    <div class="homepage-link"> 
      <a href="/mainscreen">Anasayfa</a>
    </div>
  </div>
</body>
</html>