<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Lobi Oluştur - En Yakın Tahmin Oyunu</title>
  <link rel="stylesheet" href="/css/style.css"> <!-- Genel stilleriniz için -->
  <!-- Font Awesome CDN -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap');

    body {
      font-family: 'Montserrat', sans-serif;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      color: #333;
      background: linear-gradient(-45deg, #ff8a00, #e52e71, #833ab4, #23a6d5, #23d5ab, #6a11cb, #2575fc);
      background-size: 500% 500%;
      animation: gradientAnimation 25s ease infinite;
      overflow-x: hidden;
      padding: 20px; /* Küçük ekranlar için kenar boşluğu */
      box-sizing: border-box;
    }

    @keyframes gradientAnimation {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }

    .user-actions {
        position: absolute;
        top: 20px;
        right: 20px;
        text-align: right;
        z-index: 10;
    }
    .user-actions p {
        color: white;
        margin: 0 0 8px 0;
        font-size: 0.95em;
        text-shadow: 1px 1px 2px rgba(0,0,0,0.6);
    }
    .user-actions a {
        color: #f1c40f;
        text-decoration: none;
        font-weight: bold;
        font-size: 0.9em;
        padding: 8px 12px;
        border-radius: 5px;
        background-color: rgba(0, 0, 0, 0.4);
        transition: background-color 0.3s ease, color 0.3s ease;
    }
    .user-actions a:hover {
        background-color: rgba(0, 0, 0, 0.6);
        color: #fff;
    }
    .user-actions a i {
        margin-left: 5px;
    }

    .form-container {
      background-color: rgba(255, 255, 255, 0.97); /* Hafif artırılmış opaklık */
      padding: 35px 45px;
      border-radius: 12px; /* Daha yuvarlak köşeler */
      box-shadow: 0 8px 30px rgba(0, 0, 0, 0.25);
      width: 100%;
      max-width: 480px; /* Lobi oluşturma formu için biraz daha geniş */
      text-align: center;
    }

    .form-container h2 {
      color: #2c3e50; /* Koyu mavi-gri */
      margin-top: 0;
      margin-bottom: 35px;
      font-size: 2.1em;
      font-weight: 700;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .form-container h2 i {
      margin-right: 12px;
      color: #007bff; /* Ana eylem rengiyle uyumlu ikon */
      font-size: 0.9em; /* Başlık font boyutuna göre */
    }

    .input-group {
      position: relative;
      margin-bottom: 25px;
    }
    .input-group .input-icon {
      position: absolute;
      left: 18px; /* İkonu biraz daha içeri al */
      top: 50%;
      transform: translateY(-50%);
      color: #777; /* İkon rengini biraz koyulaştır */
      font-size: 1.1em;
    }
    .input-group input[type="text"],
    .input-group select {
      width: 100%;
      padding: 16px 16px 16px 50px; /* Daha fazla padding, ikon için yer */
      border: 1px solid #ced4da; /* Standart bootstrap border rengi */
      border-radius: 8px;
      font-size: 1em;
      box-sizing: border-box;
      transition: border-color 0.3s, box-shadow 0.3s;
      color: #495057;
    }
    .input-group input[type="text"]::placeholder,
    .input-group select {
        color: #6c757d; /* Placeholder ve seçilmemiş select için renk */
    }
    .input-group input[type="text"]:read-only {
        background-color: #e9ecef; /* Bootstrap readonly arkaplanı */
        cursor: not-allowed;
    }
    .input-group input[type="text"]:focus,
    .input-group select:focus {
      outline: none;
      border-color: #80bdff; /* Bootstrap focus border rengi */
      box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25); /* Bootstrap focus shadow */
    }
    .input-group select {
        appearance: none; /* Tarayıcı varsayılan okunu kaldır */
        background-image: url('data:image/svg+xml;charset=US-ASCII,%3Csvg%20xmlns%3D%22http%3A%2F%2Fwww.w3.org%2F2000%2Fsvg%22%20width%3D%22292.4%22%20height%3D%22292.4%22%3E%3Cpath%20fill%3D%22%23007bff%22%20d%3D%22M287%2069.4a17.6%2017.6%200%200%200-13-5.4H18.4c-5%200-9.3%201.8-12.9%205.4A17.6%2017.6%200%200%200%200%2082.2c0%205%201.8%209.3%205.4%2012.9l128%20127.9c3.6%203.6%207.8%205.4%2012.8%205.4s9.2-1.8%2012.8-5.4L287%2095c3.5-3.5%205.4-7.8%205.4-12.8%200-5-1.9-9.2-5.5-12.8z%22%2F%3E%3C%2Fsvg%3E');
        background-repeat: no-repeat;
        background-position: right 1rem center;
        background-size: 0.65em auto;
        padding-right: 2.5rem; /* Ok için yer bırak */
    }


    .action-button {
      color: white;
      border: none;
      padding: 16px 28px;
      font-size: 1.05em; /* Biraz küçültülmüş font */
      font-weight: 600;
      border-radius: 8px;
      cursor: pointer;
      width: 100%;
      transition: background-color 0.2s ease-in-out, transform 0.2s ease-in-out, box-shadow 0.2s ease-in-out;
      display: flex;
      align-items: center;
      justify-content: center;
      text-decoration: none;
      box-sizing: border-box;
      text-transform: uppercase;
      letter-spacing: 0.8px;
    }
    .action-button i {
      margin-right: 10px;
      font-size: 1.25em; /* İkonları biraz büyüt */
    }
    .action-button:hover {
      transform: translateY(-3px); /* Hover efektini belirginleştir */
      box-shadow: 0 6px 12px rgba(0,0,0,0.2);
    }
    .action-button:active {
        transform: translateY(-1px);
        box-shadow: 0 3px 6px rgba(0,0,0,0.15);
    }

    .create-lobby-button {
      background-color: #28a745; /* Bootstrap success green */
      margin-bottom: 20px;
    }
    .create-lobby-button:hover {
      background-color: #218838; /* Darker green */
    }

    .lobbies-link-container {
      margin-top: 30px; /* Butonlar arası boşluğu artır */
    }
    .lobbies-link {
      background-color: #007bff; /* Bootstrap primary blue */
    }
    .lobbies-link:hover {
      background-color: #0056b3; /* Darker blue */
    }

    #create-lobby-form {
        display: flex;
        flex-direction: column;
    }
  </style>
</head>
<body class="animated-bg">

  <div class="user-actions">
      <p>Hoş geldiniz, <%= username %>!</p>
      <a href="/logout">Çıkış Yap <i class="fas fa-sign-out-alt"></i></a>
  </div>

  <div class="form-container">
    <h2><i class="fas fa-plus-square"></i>Yeni Lobi Oluştur</h2>
    <form id="create-lobby-form">
      <div class="input-group">
        <span class="input-icon"><i class="fas fa-user"></i></span>
        <input type="text" name="username" value="<%= username %>" placeholder="Kullanıcı Adınız" required readonly>
      </div>
      <div class="input-group">
        <span class="input-icon"><i class="fas fa-tasks"></i></span>
        <select name="topic" required>
          <option value="" disabled selected>Konu Seçin</option>
          <option value="Spor">Spor</option>
          <option value="Tarih">Tarih</option>
          <option value="Coğrafya">Coğrafya</option>
          <option value="Genel Kültür">Genel Kültür</option>
          <option value="Bilim">Bilim</option>
          <option value="Filmler ve Diziler">Filmler ve Diziler</option>
        </select>
      </div>
      <button type="submit" class="action-button create-lobby-button">
        <i class="fas fa-door-open"></i> Lobi Oluştur
      </button>
    </form>
    <div class="lobbies-link-container">
      <a href="/lobbies" class="action-button lobbies-link">
        <i class="fas fa-list-ul"></i> Mevcut Lobilere Göz Atın
      </a>
    </div>
  </div>

  <script src="/socket.io/socket.io.js"></script>
  <script src="/js/client.js"></script>
  <script>
    document.getElementById('create-lobby-form').addEventListener('submit', (e) => {
      e.preventDefault();
      const formData = new FormData(e.target);
      const usernameFromForm = formData.get('username'); 

      fetch('/create_lobby', {
        method: 'POST',
        headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
        body: new URLSearchParams(formData)
      })
      .then(res => res.json())
      .then(data => {
        window.location.href = `/lobby/${data.lobbyId}?username=${encodeURIComponent(usernameFromForm)}&credits=${data.credits || 0}`;
      })
      .catch(err => console.error('Lobi oluşturma hatası:', err));
    });
  </script>
</body>
</html>