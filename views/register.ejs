<!-- filepath: c:\Users\emirr\OneDrive\Masaüstü\KahootClone\views\register.ejs -->
<!DOCTYPE html>
<html lang="tr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Kayıt Ol - Kahoot!</title>
  <link rel="stylesheet" href="/css/style.css"> <!-- Genel stilleriniz varsa -->
  <!-- Font Awesome CDN for icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600;700&display=swap');

    body {
      font-family: 'Montserrat', sans-serif; /* Login sayfasıyla aynı font */
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      margin: 0;
      color: #333; /* Genel metin rengi */
      background: linear-gradient(-45deg, #ff8a00, #e52e71, #833ab4, #23a6d5, #23d5ab, #6a11cb, #2575fc); /* Login sayfasındaki gradyan */
      background-size: 500% 500%;
      animation: gradientAnimation 25s ease infinite;
      overflow-x: hidden;
    }

    @keyframes gradientAnimation { /* Login sayfasındaki animasyon */
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }

    .container {
      background-color: rgba(255, 255, 255, 0.95); /* Hafif şeffaf beyaz arka plan */
      padding: 30px 40px;
      border-radius: 10px;
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
      text-align: center;
      width: 100%;
      max-width: 420px; /* Biraz daha geniş olabilir */
      color: #333; /* Konteyner içi metin rengi */
    }

    .logo {
      font-size: 2.8em; /* Biraz daha büyük logo */
      font-weight: 700; /* Kalın font */
      color: #46178f; /* Kahoot purple */
      margin-bottom: 15px;
      line-height: 1;
    }

    .logo span {
      color: #26890c; /* Kahoot green */
    }

    h1 {
      color: #333;
      margin-bottom: 20px;
      font-size: 1.6em; /* Başlık boyutu */
      font-weight: 600;
    }

    .input-group {
      position: relative;
      margin-bottom: 18px;
    }

    .input-group i {
      position: absolute;
      left: 15px;
      top: 50%;
      transform: translateY(-50%);
      color: #999; /* İkon rengi */
      font-size: 0.9em;
    }

    input[type="text"],
    input[type="password"] {
      width: 100%; /* Tam genişlik */
      padding: 14px 15px 14px 45px; /* Sol padding ikon için ayarlandı */
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 1em;
      box-sizing: border-box;
      background-color: #f9f9f9;
      color: #333;
    }

    input[type="text"]:focus,
    input[type="password"]:focus {
      border-color: #46178f; /* Kahoot purple */
      outline: none;
      box-shadow: 0 0 0 3px rgba(70, 23, 143, 0.2);
      background-color: #fff;
    }

    button[type="submit"] {
      background-color: #26890c; /* Kahoot green */
      color: white;
      padding: 14px;
      border: none;
      border-radius: 8px;
      font-size: 1.1em;
      font-weight: 600;
      cursor: pointer;
      width: 100%;
      transition: background-color 0.2s ease, transform 0.1s ease;
      display: flex;
      align-items: center;
      justify-content: center;
      margin-top: 10px; /* Buton ile son input arasında boşluk */
    }

    button[type="submit"] i {
      margin-right: 10px; /* İkon ile yazı arasında boşluk */
    }

    button[type="submit"]:hover {
      background-color: #1e6a0a; /* Darker green */
    }
    button[type="submit"]:active {
      transform: scale(0.98); /* Butona basılma efekti */
    }

    .alternative-action {
      margin-top: 25px;
      font-size: 0.95em;
    }

    .alternative-action a {
      color: #46178f; /* Kahoot purple */
      text-decoration: none;
      font-weight: 600;
    }

    .alternative-action a:hover {
      text-decoration: underline;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="logo">
        <i class="fas fa-puzzle-piece"></i> <!-- Kahoot! yazısı yerine ikon -->
    </div>
    <h1>Hesap Oluştur</h1>
    <form action="/register" method="POST">
      <div class="input-group">
        <i class="fas fa-user"></i>
        <input type="text" name="username" placeholder="Kullanıcı Adı" required>
      </div>
      <div class="input-group">
        <i class="fas fa-lock"></i>
        <input type="password" name="password" placeholder="Şifre" required>
      </div>
      <button type="submit"><i class="fas fa-user-plus"></i>Kayıt Ol</button>
    </form>
    <div class="alternative-action">
      Zaten bir hesabın var mı? <a href="/login">Giriş Yap</a>
    </div>
        <br>
    <div class="homepage-link"> 
      <a href="/mainscreen">Anasayfa </a>
    </div>
  </div>
</body>
</html>